<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.giraffes.policykorea.board.mapper">

    <resultMap id="PostResultMap" type="com.ohgiraffers.policykorea.board.Post">
        <id property="postId" column="post_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="author" column="author"/>
        <result property="location" column="location"/>
        <result property="gender" column="gender"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="status" column="status"/>
        <result property="likes" column="likes"/>
        <result property="reports" column="reports"/>
    </resultMap>

    <select id="findAll" resultMap="PostResultMap">
        SELECT * FROM Post
    </select>

    <select id="findById" parameterType="com.ohgiraffers.policykorea.board.Post" resultMap="PostResultMap">
        SELECT * FROM Post WHERE post_id = #{id}
    </select>

    <insert id="insert" parameterType="com.ohgiraffers.policykorea.board.Post" useGeneratedKeys="true" keyProperty="postId">
        INSERT INTO Post(title, content, author, location, gender, start_time, end_time, status, likes, reports)
        VALUES(#{title}, #{content}, #{author}, #{location}, #{gender}, #{startTime}, #{endTime}, #{status}, #{likes}, #{reports})
    </insert>

    <update id="update" parameterType="com.ohgiraffers.policykorea.board.Post">
        UPDATE Post SET title=#{title}, content=#{content}, author=#{author}, location=#{location}, gender=#{gender},
                        start_time=#{startTime}, end_time=#{endTime}, status=#{status}, likes=#{likes}, reports=#{reports} WHERE post_id=#{postId}
    </update>

    <delete id="delete" parameterType="com.ohgiraffers.policykorea.board.Post">
        DELETE FROM Post WHERE post_id = #{id}
    </delete>

</mapper>
